This is a package with classic algorithm implementation

**Run Tests**  
To run all tests in the `array` package:

```sh
cd array
go test -v
```

Or run a specific test file:

```sh
go test -v binary-search_test.go
```
